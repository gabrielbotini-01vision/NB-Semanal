# Dados brutos (sensíveis: e-mails, receita linha a linha) — nunca versionar
/Dados/*.csv
!/Dados/.gitkeep

# Config local do Claude Code
.claude/settings.local.json

# Token do Astrobox (scripts/atualizar_dados.py) — nunca versionar
.env

# Cache de bytecode do Python
__pycache__/
*.pyc
